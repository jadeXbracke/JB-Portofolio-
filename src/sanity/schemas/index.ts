import { photo } from './photo';
import { series } from './series';
import { siteSettings } from './siteSettings';
import { homepage } from './homepage';

export const schemaTypes = [photo, series, siteSettings, homepage];
